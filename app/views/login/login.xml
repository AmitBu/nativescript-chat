<Page loaded="loaded">
    <StackLayout>
        <TextField hint="Username" autocorrect="false" text="{{ username }}" />
        <Button text="Login" tap="addUser" />

        <Label text="{{ username }}"></Label>
    </StackLayout>
</Page>